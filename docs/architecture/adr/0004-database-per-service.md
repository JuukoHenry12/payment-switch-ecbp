# ADR-0004: Database-per-Service (Schema-per-Service in DB2)

## Status
Proposed

## Context
Full physical database-per-service isolation vs shared DB2 instance
with separate schemas.

## Decision
(Fill in during Phase 2: started with separate schemas on one DB2 instance
for local-dev simplicity; document what a true split would require.)

## Consequences
(Fill in.)
