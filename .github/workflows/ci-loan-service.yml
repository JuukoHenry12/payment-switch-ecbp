name: CI - loan-service
on:
  push:
    paths:
      - 'services/loan-service/**'
      - 'common-dto/**'
  pull_request:
    paths:
      - 'services/loan-service/**'
jobs:
  build-test:
    uses: ./.github/workflows/reusable-build-test.yml
    with:
      service-path: services/loan-service
